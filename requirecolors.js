const chalk = require('chalk');
console.log(chalk.red('chalk  Text in red'));
const colors = require('colors/safe');
console.log(colors.red('colors  Text in red'));
console.log(colors.blue('this text is blue'));